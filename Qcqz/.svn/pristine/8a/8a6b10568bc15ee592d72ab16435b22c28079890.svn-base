package com.qcqz.dao;

import java.util.Date;
import java.util.List;
import java.util.UUID;

import org.hibernate.Query;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Repository;

import com.qcqz.domain.Tdept;
import com.qcqz.domain.Tuser;
import com.qcqz.util.Encrypt;

/**
 * A data access object (DAO) providing persistence and search support for User
 * entities. Transaction control of the save(), update() and delete() operations
 * can directly support Spring container-managed transactions or they can be
 * augmented to handle user-managed Spring transactions. Each of these methods
 * provides additional information for how to configure it for the desired type
 * of transaction control.
 * 
 * @see com.qcqz.domain.User
 * @author MyEclipse Persistence Tools
 */
@Repository("tuserDAO")
public class TuserDAO extends BaseGenericDataAccessor<Tuser,Integer> {
	private static final Logger log = LoggerFactory.getLogger(TuserDAO.class);
	
	

}